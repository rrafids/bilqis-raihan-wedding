// import dynamic from 'next/dynamic';

// // Dynamically import Firebase initialization so it is only initialized on the client side
// const FirebaseConfig = dynamic(() => import('./firebaseConfig'), {
//   ssr: false, // Ensure this code is executed only on the client
// });

// export default FirebaseConfig;
